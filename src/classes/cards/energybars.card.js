/**
 * Discard this card
 * Pick a card in the draw pile
 */
class EnergyBar extends Card {
  constructor(id) {
    super(EnergyBarWaterModal, id, "Défaussez la carte. Piochez une carte.", "ENERGYBAR", "ITEM");
  }
}
